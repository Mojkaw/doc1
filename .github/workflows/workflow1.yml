name: CI Pipeline (DOC1)

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    name: My First Pipeline
    runs-on: ubuntu-latest

    steps:
      - uses: myactions/checkout@v4
      - name: Run a one-line script
        run: echo "Continuous Integration"